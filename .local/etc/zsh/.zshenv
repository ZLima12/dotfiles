# Default Programs
export BROWSER="qutebrowser"
export DIFFPROG="meld" # Used by pacdiff, at least.
export EDITOR="nvim"
export TERMINAL="st"
export PAGER="less"

# Path
export PATH="${HOME}/.local/bin:$PATH" # Might not be necessary; Arch seems to set .local/bin automatically.
